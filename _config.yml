title: ITCNE23-SEM-lll
description: Description of the site 
remote_theme: just-the-docs/just-the-docs
#remote_theme: tomjoht/documentation-theme-jekyll
#remote_theme: dieghernan/chulapa

# Color scheme supports "light" (default) and "dark"
# color_scheme: dark

favicon_ico: "/docs/img/favorite.png"

url: https://danyambuehl.github.io/ITCNE23-SEM-lll

# Set a path/url to a logo that will be displayed instead of the title
logo: "/docs/img/logo.png"

# Aux links for the upper right navigation
aux_links:
  Projektplanung: https://github.com/users/danyambuehl/projects/3

# Makes Aux links open in a new tab. Default is false
aux_links_new_tab: true

footer_content: "Your Copyright Here  "

# For copy button on code
enable_copy_code_button: true

# By default, consuming the theme as a gem leaves mermaid disabled; it is opt-in https://just-the-docs.com/docs/ui-components/code/
mermaid:
  # Version of mermaid library
  # Pick an available version from https://cdn.jsdelivr.net/npm/mermaid/
  #version: "9.1.6"
  version: "10.0.2"
  path: "https://cdn.jsdelivr.net/npm/mermaid@10/dist/mermaid.min.js"  # Use the CDN for the latest version
  # Put any additional configuration, such as setting the theme, in _includes/mermaid_config.js
  # See also docs/ui-components/code
  # To load mermaid from a local library, also use the `path` key to specify the location of the library; e.g.
  # for (v10+):
  # path: "/assets/js/mermaid.esm.min.mjs"
  # for (<v10):
  # path: "/assets/js/mermaid.min.js"
  # Note: copy both `mermaid.esm.min.mjs` (v10+) or `mermaid.min.js` (<v10) and the associated `.map` file from the specified version of `mermaid/dist` to `/assets/js/`.

# Back to top link
back_to_top: true
back_to_top_text: "Back to top ⬆️"

# Footer last edited timestamp
last_edit_timestamp: true # show or hide edit time - page must have `last_modified_date` defined in the frontmatter
last_edit_time_format: "%b %e %Y at %I:%M %p" # uses ruby's time format: https://ruby-doc.org/stdlib-2.7.0/libdoc/time/rdoc/Time.htm

search_enabled: true
search:
  # Split pages into sections that can be searched individually
  # Supports 1 - 6, default: 2
  heading_level: 2
  # Maximum amount of previews per search result
  # Default: 3
  previews: 2
  # Maximum amount of words to display before a matched word in the preview
  # Default: 5